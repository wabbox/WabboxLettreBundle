{% extends "WabboxLettreBundle:Default:index.html.twig" %}  
    {% block containt %}
<h3>Contenu edit</h3>

<form action="{{ path('contenu_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Modifier</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('contenu') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <form action="{{ path('contenu_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="button radius">Supprimer</button>
        </form>
    </li>
</ul>
{% endblock %}